import { HeaderContainer, Toolbar } from './style';

const Header = ({ title }) => {
  return (
    <HeaderContainer>
      <Toolbar>
      </Toolbar>
    </HeaderContainer>
  );
};

export default Header;
